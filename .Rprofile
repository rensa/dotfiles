# james's rprofile

options(error = recover)
options(stringsAsFactors = FALSE)
options(editor = 'vim')
options(width = 80)

if(Sys.getenv('TERM') == 'xterm-256color')
    library(colorout)

.First = function()
{  
    
}